# Alert

Inline message block with a left keyline and icon.

```jsx
<Alert variant="brass" title="Specialist assigned">Thandi will email you within the hour.</Alert>
<Alert variant="warning" title="Only 2 rooms left" onClose={...}>For your October dates.</Alert>
```

variants: `brass` · `info` · `success` · `warning` · `danger`.