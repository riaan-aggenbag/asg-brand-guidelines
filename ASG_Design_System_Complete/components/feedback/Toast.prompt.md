# Toast

Transient confirmation card. Render inside a fixed-position wrapper (bottom-right).

```jsx
<Toast variant="success" title="Itinerary saved" onClose={...}>We've emailed you a copy.</Toast>
```